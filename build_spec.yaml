version: 0.1             
component: build
timeoutInSeconds: 500
shell: bash           

steps:
  - type: Command
    name: "Build app"
    command: |
      echo "Hello from here"
outputArtifacts:
  - name: node-app-deployment.yml
    type: BINARY
    location: node-app-deployment.yml:1
